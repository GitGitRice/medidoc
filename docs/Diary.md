# Projekttagebuch

Täglicher Stand pro Person. Neueste Einträge oben.

| Datum      | Person | Ergebnisse |
|------------| ------ | ---------- |
| 2026-08-12 | Alexander Schmidt | Dokument ändern wird jetzt mit dem Backend verbunden. Und benutzt die **`PATCH /docs/{patientId}/{docId}`** 
Funktion.
| 2026-08-12 | Tiran Grigoryan | **`PATCH /docs/{patientId}/{docId}`** ergänzt — Dokumente ließen sich bisher nur anlegen, auflisten und löschen, aber nicht korrigieren. Rumpf ist **JSON statt multipart**: Beim Anlegen reist ein Anhang mit, beim Ändern nicht. Weggelassene Felder bleiben stehen, `document_type` und `title` dürfen nicht geleert werden (sonst entstünde über `PATCH` ein Dokument, das `POST` nie erlaubt hätte), und `fields` **ersetzt** statt zusammenzuführen — sonst liesse sich ein Schlüssel nie wieder entfernen. Weder Patient noch Anhang sind änderbar. Neues Feld `updated_at` (`null`, solange nie geändert) und ein `document_updated` im Audit-Trail, das die geänderten **Feldnamen** festhält, nicht die Werte. `staff` darf ändern — ADR-0005 nennt das nicht ausdrücklich, **gehört ins Daily**. 23 neue Tests (287 gesamt). Dabei gefunden: `created_at` kam beim Anlegen mit Mikrosekunden zurück, aus MongoDB später nur mit Millisekunden — Zeitstempel werden jetzt vor dem Schreiben gekürzt. |
| 2026-08-11 | Farhad Sababi | **Patient löschen mit Rückfrage** umgesetzt (Issue #22): Die Löschen-Aktion sitzt auf der Patientendetailseite (`PatientDetail.jsx`), nicht in der Übersichtstabelle, wie im Issue verlangt — sichtbar nur für `admin` (ADR-0005), `staff` sieht sie nicht. Ein Klick öffnet den bestehenden `DeleteConfirmDialog.jsx` (bisher nur für Dokumente genutzt) mit dem vollen Namen des Patienten in der Rückfrage. Bestätigen ruft `DELETE /patients/{id}` über den bestehenden `patientPath(id)` und das authentifizierte `apiFetch` auf; bei Erfolg (`204`) schließt der Dialog und `PatientDetailPage.jsx` navigiert zur Übersicht. Scheitert das Löschen, schließt der Dialog ebenfalls, aber die Seite bleibt stehen und zeigt `ApiError.message` — bewusst kein automatischer Retry, weil ein `500` beim Aufräumen der Akte den Patienten in Postgres bereits gelöscht haben kann (docs/patients-api.md). 133/133 Frontend-Tests grün, `npm run build` erfolgreich. |
| 2026-08-10 | Farhad Sababi | **Patient anlegen und bearbeiten** umgesetzt (Issue #21): `PatientFormDialog.jsx` als ein gemeinsames Formular für beide Fälle, unterschieden nur durch `mode` ("create" \| "edit") — dasselbe Formular für Anlegen und Bearbeiten, wie im Issue verlangt. „Neuer Patient" sitzt als runder Fab in `PatientToolbar.jsx` neben der Suche (nur `staff`/`admin`, ADR-0005); Bearbeiten kommt als zweite Aktion in `PatientActions.jsx` dazu und lädt zuerst die vollen Patientendaten über `GET /patients/{id}` nach, weil die Tabellenzeile nur die schmale Form kennt. Speichern ruft je nach Modus `POST /patients` oder `PATCH /patients/{id}` auf, schließt den Dialog, zeigt eine Erfolgsmeldung per Snackbar und lädt die Liste neu. Pflichtfelder werden vor dem Absenden geprüft, Fehler vom Backend — inklusive Feldfehlern aus `422` — werden im Dialog angezeigt, nicht verschluckt; `api.js` bekommt dafür `ApiError.errors` und `patientPath(id)`. 108/108 Frontend-Tests grün, `npm run build` erfolgreich. |
| 2026-08-09 | Farhad Sababi | **Suche in der Patientenübersicht** umgesetzt (Issue #19): `PatientSearch.jsx` als eigenes MUI-Suchfeld über der Patiententabelle ergänzt; Suchanfragen nach Vor- und Nachname werden über den bestehenden `q`-Parameter von `GET /patients` an das Backend übergeben. Die Eingabe wird um 300 ms entprellt, erst danach geht die Anfrage raus. Eine neue Suche setzt die Seitennavigation auf die erste Seite zurück. Findet die Suche nichts, zeigt die Übersicht eine eigene Meldung („Keine Treffer") statt der allgemeinen Leerzustand-Meldung; das Leeren des Suchfelds zeigt wieder alle Patienten. Die bestehende `AbortController`-Logik zum Abbrechen veralteter Anfragen bleibt unverändert. 62/62 Frontend-Tests grün, `npm run build` erfolgreich. |
| 2026-08-09 | Alexander Schmidt | Dokument neu anlegen und das Layout von Dokument bearbeiten angepasst. Das Suchen-Layout bei den Dokumenten bearbeitet und nur den Löschen-Button zeigen, wenn der Benutzer Administrator ist.
| 2026-08-09 | Steven Tanu | **Admin-Seite Benutzerverwaltung** umgesetzt (Issue #54): `/users` im Frontend, nur mit `admin` erreichbar. Aufbau wie die Patientenübersicht — dieselbe Kopfzeile, dieselbe MUI-Tabelle, dieselbe Seitensteuerung; Farben kommen aus dem Theme (`action.hover`, `text.secondary`, `color="success"`), nirgends ein fester Farbwert. Rolle je Zeile über ein natives `<select>` änderbar, Deaktivieren erst nach Rückfrage, deaktivierte Benutzer bleiben sichtbar und lassen sich zurückholen. Fehler des Backends stehen dort, wo sie hingehören: eine misslungene Änderung als Meldung über der Tabelle (die Liste bleibt stehen), ein abgelehntes Anlegen im Dialog, damit die Eingabe nicht verloren geht. Am eigenen Konto sind Rolle und Deaktivieren gesperrt — dieselbe Regel, die das Backend mit `409` durchsetzt, nur bevor man dagegenläuft. Dazu die Rollenprüfung als eigene Route (`RoleRoute`): Wem die Rolle fehlt, sieht die Meldung im gewohnten Rahmen statt einer Weiterleitung zur Anmeldung — `401` und `403` sind verschiedene Dinge. In der Patientenübersicht führt ein Knopf dorthin, sichtbar nur für `admin`. **Nachgezogen in #50:** `GET /users` gab es noch nicht, ohne Liste hätte die Seite niemanden zum Ändern anbieten können; Form ist `UserPage` wie `PatientPage`. Die deutsche Beschriftung der Seitensteuerung liegt jetzt einmal in `PaginationFooter.jsx` statt in jeder Tabelle. 74 Frontend-Tests, 157 im Backend, `npm run build` läuft durch. **Nebenbefund:** Auf `develop` liegt `frontend/node_modules` als eingecheckter Symlink auf sich selbst (aus 020007d) — er zerstört bei jedem Merge die lokale Installation; auf diesem Branch entfernt. |
| 2026-08-08 | Alexander Schmidt | Im Frontend die PatientenDetailSeite erstellt mit der Patientenüberischt und einer Liste der Dokumente die zu dem Patienten gehören. Die Dokumente, können in der Liste bearbeitet und gelöscht werden. Über der Liste gibt es auch eine Suchfunktion die nach dem Dokumenttitel bzw. nach dem Tagnamen sucht und filtert. Beim Löschen kommt auch ein Dialog, welches fragt ob man wirklich löschen möchte. Die Datenbankabfrage muss noch ergänzt werden. In einem anderen Issue wird dann auch der Button "Anhang laden" mit Logik implementiert. Error-Meldung implementiert. |
| 2026-08-08 | Steven Tanu | **Review zu PR #48** entlang zweier Achsen (Standards und Spec) und die Befunde abgearbeitet. Zwei Blocker: Docker Compose wartete nur noch auf `service_started` statt `service_healthy` bei MongoDB — seit den Dokumenten hält Mongo nicht mehr nur den Trail, das Gatter ist zurück; und `DELETE /patients/{id}` schrieb den Trail-Eintrag **nach** dem Abräumen der Akte, ein Mongo-Ausfall hätte damit den einzigen Beleg verschluckt, dass es den Patienten gab. Dazu die **Namensfrage entschieden: `CONTEXT.md` gilt.** Ein Dokument hat jetzt einen `document_type` und typabhängige `fields`, der **Anhang ist optional** (`attachment`, `null` erlaubt) — damit trägt auch die Begründung von ADR-0002 wieder, die sich ausdrücklich auf heterogene Angaben stützt. `filename` erfindet keinen `<id>.bin` mehr, beide Speicher teilen sich dasselbe Suchprädikat, deutsche Bezeichner raus. Zweite Runde desselben Reviews, drei Befunde nachgezogen: **Anhänge können beim Löschen eines Patienten nicht mehr verwaisen** — die Platte wird jetzt auch dann abgeräumt, wenn schon die Angaben nicht wegzubekommen waren, und ein gescheitertes Abräumen wird nicht länger als `204` gemeldet; **kein stiller Rückfall in den Speicher**, ohne `MONGO_URL` antworten die Dokument-Endpunkte mit `500` statt ein `201` auszustellen, dessen Dokument den Neustart nicht überlebt (Nachtrag in ADR-0007); und **`fields` kürzt zu lange Schlüssel und Werte nicht mehr still**, sondern weist sie mit `422` ab — so, wie der Vertrag es ohnehin schon behauptet hat. 99 Tests im Dokumente-Modul, 223 insgesamt grün. |
| 2026-08-08 | Farhad Sababi | **Patientenübersicht als Tabelle** umgesetzt (Issue #18): `Overview.jsx` lädt über `GET /patients` mit `limit`/`offset` und übernimmt die vom Backend gelieferte Reihenfolge unverändert, kein eigenes Sortieren im Frontend. Tabelle mit den Spalten Nachname, Vorname, Geburtsdatum, Versicherung und Aktionen, Paginierung über MUI `TablePagination` mit deutscher Beschriftung, dazu Lade-, Fehler- und Leerzustand. Zeilenklick und ein eigener Details-Button navigieren zur Patientendetailseite. Tabelle und Aktionen-Zelle in eigene Komponenten ausgelagert (`PatientTable.jsx`, `PatientActions.jsx`); `PatientActions` enthält für #18 bewusst nur Details — Bearbeiten, Löschen, Suche und der Hell/Dunkel-Modus sind kein Teil dieses Issues und folgen mit #21, #22, #19 beziehungsweise separat. Tests für Lade-/Fehler-/Leerzustand, Spalten, Reihenfolge, Paginierung und Navigation ergänzt, `npm test` und `npm run build` laufen durch. |
| 2026-08-07 | Dominik Fischer | AWS-Demo-Deployment für Issue #51 eingerichtet und dokumentiert: vollständiger Docker-Compose-Stack auf einer Amazon-Linux-EC2-Instanz, Administration ausschließlich über Systems Manager Session Manager, stabile Adresse per Elastic IP sowie automatischer Bootstrap mit Docker Compose, Buildx, Swap, zufälligen Demo-Zugangsdaten und Seed-Daten. Einrichtung, Betrieb, Aktualisierung und vollständiger Rückbau sind in [docs/aws-deployment.md](./aws-deployment.md) beschrieben. |
| 2026-08-06 | Tiran Grigoryan | **Dokumente je Patient** umgesetzt ([docs/documents-api.md](./documents-api.md)): `POST /docs/{patientId}`, Liste mit `?q=` über Titel und Beschreibung, `DELETE` nur für `admin`. Angaben in MongoDB, Anhänge auf einem eigenen Volume (ADR-0002). Größe wird beim Schreiben geprüft, nicht danach — über 20 MB kommt `413`, ohne dass etwas liegen bleibt. Der Dateiname des Aufrufers wird nie zum Pfad. Wird ein Patient gelöscht, geht seine Akte mit — sonst blieben Anhänge unerreichbar liegen. 67 neue Tests. Offen fürs Daily: `CONTEXT.md` nennt die Datei einen **Anhang** und das Dokument etwas mit typabhängigen Feldern — dieser Endpunkt macht es umgekehrt. *(Entschieden am 08.08.: CONTEXT.md gilt, siehe Eintrag oben.)* |
| 2026-08-05 | Steven Tanu | Auth-Strang abgeschlossen und über `feature/auth` nach `develop` gemerged (PR #31): Patienten-Endpunkte am Router mit `get_current_user` abgesichert, `DELETE` zusätzlich mit `require_roles(Role.ADMIN)` (Issue #15), Login-Seite und `AuthContext` mit `ProtectedRoute`, Token in `localStorage` und Wiederherstellung über `GET /auth/me` (Issue #17). Dazu GitHub Actions für beide Test-Suiten. Danach aufgeräumt (Issue #35): `npm run build` als CI-Schritt, veraltete Stellen in `docs/patients-api.md` und im Patienten-Router korrigiert, Tagebuch auf diese Datei vereinheitlicht. Beim Mergen von `develop` die Namensfrage entschieden: Pfad und Query-Parameter sind wieder englisch (`/patients`, `?q=`), deutsch bleiben Kommentare, Doku und Oberfläche — so wie ADR-0005 und die beiden API-Verträge es ohnehin schreiben. Dazu den Rollen-Helfer `hasRole` an `useAuth()` und eine feste Meldung für `403` (Issue #34, PR #40) — der Löschen-Button aus #22 kann ihn direkt benutzen, statt `user.role` selbst auseinanderzunehmen. **Hindernis/offen:** `main` und `develop` sind auf GitHub nicht geschützt — ADR-0006 verlangt Pull Requests, erzwungen wird es bisher nicht. |
| 2026-08-05 | Dominik Fischer | React/Vite-Frontend für die lokale Entwicklung containerisiert: Dockerfile und `.dockerignore` ergänzt, den Frontend-Service mit Hot Reload und eigenem `node_modules`-Volume in Docker Compose eingebunden sowie `VITE_API_URL` und den konfigurierbaren Frontend-Port dokumentiert. Der vollständige Stack aus PostgreSQL, FastAPI und Frontend lässt sich jetzt mit `docker compose up --build` starten (Issue #4). |
| 2026-08-05 | Tiran Grigoryan | **Logging- und Monitoring-Konzept** umgesetzt ([docs/logging-monitoring.md](./logging-monitoring.md)), drei Schichten: eine Logzeile pro Anfrage auf stdout (mit Anfrage-Kennung `X-Request-ID`, Dauer und Benutzer), ein dauerhafter **Audit-Trail in MongoDB** (Anmeldeversuche, abgewiesene Token, abgewehrte Zugriffe, Änderungen an Patienten) und eine **Missbrauchserkennung**, die über den Trail zählt — u. a. Brute Force je Konto und ID-Enumeration auf Patientenakten. Es wird bewusst **nur erkannt, nie blockiert**. Redis und MongoDB vorher gemessen; Redis ist ~2× schneller, MongoDB gewinnt trotzdem, weil der Trail auswertbar und dauerhaft sein muss und über ADR-0002 ohnehin gesetzt ist ([ADR-0007](./adr/0007-mongodb-fuer-audit-und-monitoring.md)). Kein Passwort, kein Token und keine Patientendaten landen im Trail — dafür eigene Tests. 43 neue Tests, `mongo`-Service in Docker Compose. Ohne `MONGO_URL` läuft alles weiter, Trail dann im Speicher. |
| 2026-08-04 | Farhad Sababi | Gemeinsames Layout (Kopfzeile, Navigation) ergänzt und in `App.jsx` um die geschützten Seiten gelegt; Platzhalter-Route `/patients/:patientId` für die künftige Patientendetailseite eingebunden — schließt die verbliebenen Punkte aus Issue #16 (PR #33). |
| 2026-08-04 | Dominik Fischer | Entwicklungsumgebung für das FastAPI-Backend containerisiert: Dockerfile und `.dockerignore` ergänzt, den Backend-Service in Docker Compose auf einen eigenen Build umgestellt und die benötigten Entwicklungsvariablen in `.env.example` dokumentiert (Issue #3, PR #28). |
| 2026-08-04 | Tiran Grigoryan | Patienten-Endpunkte fertiggestellt und mit **55 automatisierten Tests** abgedeckt ([tests/test_patients_api.py](../backend/tests/test_patients_api.py), ein Block je Endpunkt). Dazu ein **einheitliches Fehlerformat für die ganze API** (`status` und `message`, bei `422` zusätzlich `errors` mit Feldnamen) in `app/core/errors.py` — `detail` bleibt übergangsweise erhalten, damit nichts im Frontend bricht. Meldungen nennen jetzt den konkreten Wert („Patient mit der ID 42 wurde nicht gefunden"). Suchparameter auf `?suche=` gezogen, passend zum Pfad `/patienten`. [docs/patients-api.md](./patients-api.md) auf den neuen Stand, `auth-api.md` und beide READMEs nachgezogen. |
| 2026-08-04 | Steven Tanu | `POST /auth/login` umgesetzt: JWT mit `sub`, `role` und acht Stunden Laufzeit, einheitliches `401` für unbekannte E-Mail, falsches Passwort und deaktivierten Benutzer (Issue #14). Dazu das erste Test-Setup im Backend (pytest gegen SQLite im Speicher, 10 Tests), E-Mails jetzt case-insensitiv, fehlende Variablen in `.env.example` ergänzt und Gitflow als Branching-Modell dokumentiert (ADR-0006). |
| 2026-08-03 | Tiran Grigoryan | Backend nach **Features** umstrukturiert: `app/modules/<feature>/` mit immer denselben Dateien (`models`, `schemas`, `service`, `router`, `seed`), `core/` und `db/` fachlich neutral, ein Modul-Router pro Zeile in `api/router.py`. Tragende Regel: `service.py` kennt kein HTTP. Dabei den halbfertigen Umbau repariert (kollidierendes `app/db.py` neben `app/db/`, falsches `REPO_ROOT`, fehlende `JWT_SECRET`/`SEED_*` in `.env.example`, Compose-Mount auf `./app` statt `./backend`). Patientenverwaltung umgesetzt — `Patient`-Model, Schemas mit Validierung, CRUD mit Suche und Paging — plus **200 erfundene Testpatienten** als [backend/testdata/patients.json](../backend/testdata/patients.json), aus der der Seed lädt (`python -m app.seed --patients 50` für weniger). |
| 2026-08-03 | Dominik Fischer | PostgreSQL-Service mit Docker Compose, persistentem Volume, `.env.example` und Healthcheck umgesetzt und die Datenpersistenz nach einem Container-Neustart geprüft (Issue #2, PR #23). |
| 2026-08-03 | Steven Tanu | Backend-Grundgerüst, Benutzermodell mit bcrypt-Hashing und Seed umgesetzt (Issue #13, PR #24). |

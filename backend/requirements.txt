# Untergrenze, keine Festnagelung. Ab 0.141 bringt Starlette 1.x die
# Statuskonstanten unter ihren neuen Namen mit — HTTP_413_CONTENT_TOO_LARGE
# und HTTP_422_UNPROCESSABLE_CONTENT. Der Code benutzt ausschliesslich diese;
# die abgekuendigten Schreibweisen (…_REQUEST_ENTITY_TOO_LARGE,
# …_UNPROCESSABLE_ENTITY) kommen nirgends mehr vor, auch nicht ueber
# http.HTTPStatus. Ein kuenftiger Wegfall der alten Namen trifft uns damit
# nicht, und die Obergrenze darf offen bleiben, bis 1.0 kommt.
fastapi>=0.141,<1.0
uvicorn[standard]
sqlmodel
psycopg[binary]
pydantic-settings

# Auth
pyjwt
bcrypt>=4.0
python-multipart

# Audit-Trail in MongoDB. Ohne MONGO_URL laeuft der Trail im Speicher, die
# Anwendung startet also auch ohne erreichbares Mongo (app/modules/audit/store.py).
pymongo

# Tests. Laufen gegen SQLite im Speicher, brauchen also keine eigene Datenbank.
# httpx2 statt httpx: Starlettes TestClient warnt sonst bei jedem Lauf. Es ist
# die Nachfolgelinie von httpx (github.com/pydantic/httpx2), kein fremder Fork.
# Als einzige Abhängigkeit hier auf die Major-Version gepinnt, weil der Name
# sonst zur Verwechslung einlädt.
pytest
httpx2>=2,<3

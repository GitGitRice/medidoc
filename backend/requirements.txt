fastapi
uvicorn[standard]
sqlmodel
psycopg[binary]
pydantic-settings

# Auth
pyjwt
bcrypt>=4.0
python-multipart

# Tests. Laufen gegen SQLite im Speicher, brauchen also keine eigene Datenbank.
# httpx2 statt httpx: Starlettes TestClient warnt sonst bei jedem Lauf.
pytest
httpx2
